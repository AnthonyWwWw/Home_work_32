import { styled } from '@mui/system'
import { Container } from '@mui/material'

export const ContentConteiner = styled(Container)({
    display: 'flex', 
    flexDirection: 'column', 
    height: '100vh'
})
